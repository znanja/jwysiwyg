(function( Wysiwyg ) {
//
// Functions for handling user selection data
//
// Gets the current selection range
//
Wysiwyg.fn.getRange = function(){
	
};

// Gets the current user selection
//
Wysiwyg.fn.getSelection = function(){
	
};

})( Wysiwyg );